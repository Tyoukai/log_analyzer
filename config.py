# -*- coding: utf-8 -*-
"""
配置中心 - 集中管理所有可配置项
"""

# =============================================================================
# Kafka 配置
# =============================================================================
KAFKA_CONFIG = {
    # 全局开关：是否开启 Kafka 异步订阅训练。
    # 训练完成后可将其改为 False，关闭 Kafka 消费。
    "enable_consumer": True,
    
    # Kafka 集群地址（5 节点集群），部署时替换为真实 IP
    "bootstrap_servers": [
        "172.19.185.25:9092",
        "172.19.185.26:9092",
        "172.19.185.27:9092",
    ],
    # 订阅的日志 Topic
    "topic": "dzg",
    # 消费者组 ID
    "group_id": "pangu-log-template-trainer",
    # 消费起始位置：earliest 从头消费 / latest 只消费新消息
    "auto_offset_reset": "latest",
}

# =============================================================================
# HTTP 服务配置
# =============================================================================
HTTP_CONFIG = {
    "host": "0.0.0.0",
    "port": 8000,
}

# =============================================================================
# Drain3 持久化配置
# =============================================================================
PERSISTENCE_CONFIG = {
    # 持久化文件路径（与代码同级目录）
    "state_file": "drain3_state.bin",
}
